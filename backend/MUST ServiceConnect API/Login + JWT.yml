info:
  name: Login + JWT.
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
